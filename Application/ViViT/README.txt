It gets non-random validation score from 30-35% due to low number of videos per class (validation set is tiny).
Train acc was 100% at the Epoch 5, so it means that model can fit the training data.

The process went like this: Pretrained ViViT -> fine-tuned -> saved model -> code for webcam
